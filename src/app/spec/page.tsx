import { SpecReferenceClient } from "./SpecReferenceClient";

export const metadata = {
  title: "Spec Reference | Toolroom",
  description: "Enviropax Standard PET Tooling Manufacture Specification — KMD 78.2 Speed",
};

export default function SpecReferencePage() {
  return <SpecReferenceClient />;
}
