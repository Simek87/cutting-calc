import { FusionRefsClient } from "./FusionRefsClient";

export const metadata = {
  title: "Fusion References | Toolroom",
  description: "CAM program library — Fusion 360 examples by component type",
};

export default function FusionRefsPage() {
  return <FusionRefsClient />;
}
